# Minimal dependencies for the AIEO MCP server
# (no database, no celery, no spacy — just scoring + MCP)

# MCP protocol
mcp>=1.0.0

# Content parsing
beautifulsoup4>=4.12.0
markdown>=3.5.0
html2text>=2024.2.0

# HTTP client (for aieo_audit_url tool)
httpx>=0.27.0
certifi>=2024.0.0

# AI providers (for AI-driven scoring)
openai>=1.12.0
anthropic>=0.18.0

# Pydantic for config
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Screenshot capture (for visual analysis)
playwright>=1.40.0
